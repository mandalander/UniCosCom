{
  "auth": {
    "providers": [
      "password",
      "google.com"
    ]
  },
  "firestore": {
    "/users/{userId}": {
      "schema": {
        "$ref": "#/backend/entities/user"
      },
      "description": "Collection to store user profiles. Authorization relies on path-based ownership (only the user can read/write their own profile)."
    }
  }
}